# CORTEX Desktop Configuration

# CORTEX server host (localhost only for security)
CORTEX_HOST=127.0.0.1

# CORTEX server port (must match server.mjs)
CORTEX_PORT=7002

# Development mode (shows DevTools)
NODE_ENV=production
